{
  "name": "uk-broadband-checker-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AWS Lambda handler for UK broadband lookups (Zero-Trust, GDPR-safe, cache-aside).",
  "main": "src/handler.mjs",
  "scripts": {
    "lint": "node --check src/handler.mjs",
    "test": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.726.0",
    "@aws-sdk/client-ssm": "^3.726.0",
    "@aws-sdk/lib-dynamodb": "^3.726.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "vitest": "^2.1.8"
  }
}
